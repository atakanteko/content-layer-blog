import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
    token: {
        fontSize: 16,
    },
    components: {
        Switch: {
            handleBg: "#eea517"
        }
    }
};

export default theme;